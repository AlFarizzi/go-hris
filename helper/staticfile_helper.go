package helper

// func StaticFile(public *embed.FS, router *httprouter.Router) {
// 	dir, err := fs.Sub(public, "public")
// 	PanicHandler(err)
// 	fileServer := http.FileServer(http.FS(dir))

// 	router.hanle

// 	http.Handle("/static/", http.StripPrefix("/static", fileServer))
// }
